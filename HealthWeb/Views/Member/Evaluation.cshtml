@using Syncfusion.EJ2
@using Syncfusion.EJ2.Navigations
@{
    ViewBag.Title = "Evaluation";
    Layout = "~/Views/Shared/_LayoutKinHealthMemberDemo.cshtml";
}

@{ 
    ViewBag.PartialPage = true;
}

<div class="row">
    @*<div class="col-lg-3">
        @Html.EJS().Menu("evalMenu").Items(i =>
        {
            i.Text("生理量測資料").Url("/Member/Evaluation").Id("evalVitalSigns").IconCss("demo-icons demo-icon-measurement").Add();
            i.Text("健康目標達成狀況").Url("/Member/Evaluation/2").Id("evalSummary").IconCss("demo-icons demo-icon-measurement").Add();
            i.Text("個人健康狀態").Url("/Member/Evaluation/3").Id("evalSport").IconCss("demo-icons demo-icon-measurement").Add();
            i.Text("運動評量").Url("/Member/Evaluation/4").Id("evalSummary").IconCss("demo-icons demo-icon-measurement").Add();
            i.Text("健康風險資訊與提醒").Url("/Member/Evaluation/5").Id("evalSport").IconCss("demo-icons demo-icon-measurement").Add();
        }).Orientation(Orientation.Vertical).Render()
    </div>*@
    <div class="col-lg-12">
        @{
            switch (ViewBag.PageId)
            {
                case "1":
                    @Html.Partial("VitalSigns")
                    break;
                case "2":
                    @Html.Partial("EvalTarget")
                    break;
                case "3":
                    @Html.Partial("EvalRadar")
                    break;
                case "4":
                    @Html.Partial("EvalSport")
                    break;
                case "5":
                    @Html.Partial("EvalRisk")
                    break;
                case "6":
                    @Html.Partial("EvalSchedule")
                    @section scripts
                    {
                        <script type="text/javascript">
                            $(function () {
                                showReminder();
                            });
                        </script>
                    }

                    break;
            }
        }
    </div>
</div>
